{
  "name": "@petfinder/components-library",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "dependencies": {
    "@material-ui/core": "4.11.0",
    "@types/react-lazyload": "^3.0.0",
    "clsx": "^1.1.1",
    "react-jss": "^10.5.0",
    "react-lazyload": "^3.0.0"
  },
  "scripts": {
    "build": "tsc -b .",
    "build:clean": "rm -rf ./dist && rm ./tsconfig.tsbuildinfo && yarn build"
  },
  "peerDependencies": {
    "react": "^16.12.0"
  }
}
