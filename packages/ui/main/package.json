{
  "name": "@petfinder/main-ui",
  "version": "1.0.0",
  "private": true,
  "main": "dist/main.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p ./tsconfig.server.json",
    "bundle": "SKIP_PREFLIGHT_CHECK=true REACT_APP_STAGE=$STAGE env-cmd -f ./.env.$STAGE --fallback react-scripts build",
    "eject": "react-scripts eject",
    "start": "react-scripts start"
  },
  "dependencies": {
    "clsx": "^1.1.1",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "^3.3.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
